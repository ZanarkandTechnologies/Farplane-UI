## Non-secret UI bootstrap values only.
# Credential-bearing VITE_* names are documented in the root `.env.example`
# and must be injected by Doppler when launching the UI.
VITE_GATEWAY_URL=http://127.0.0.1:18789
VITE_STATE_URL=http://127.0.0.1:5173
