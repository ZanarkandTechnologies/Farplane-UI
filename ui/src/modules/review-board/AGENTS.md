# Review Board Module

- Keep filesystem-ticket review normalization here. Empty or unavailable
  sources must remain explicit; do not substitute mock tasks for live state.
- Team kanban rendering belongs in `modules/team-workspace`.
- Runtime adapter models come from `modules/runtime`.
