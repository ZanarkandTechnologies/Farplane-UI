# Review Board Module

- Keep review task normalization and mock/fallback review-board data here.
- Team kanban rendering belongs in `modules/team-workspace`.
- Runtime adapter models come from `modules/runtime`.

