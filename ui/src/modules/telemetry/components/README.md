# Telemetry Components

Telemetry-owned presentation components live here: dashboard charts, usage tables, metric ticker chrome, and component-local CSS.

Shared shadcn-style primitives stay in `ui/src/components/ui`. Module-level query containers, types, and formatting helpers stay one directory up so global and team telemetry entrypoints can share them.
