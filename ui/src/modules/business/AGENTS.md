# Business Module

- Keep business drafting and resource conversion helpers here.
- UI components that only render a team panel stay in `modules/team-workspace`.
- Runtime transport code stays in `modules/runtime`; import business contracts from this module when endpoint payloads need them.

