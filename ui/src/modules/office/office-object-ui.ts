"use client";

/**
 * OFFICE OBJECT UI COMPATIBILITY FACADE
 * =====================================
 * Public legacy import path for office object interaction metadata helpers.
 *
 * KEY CONCEPTS:
 * - Real implementation lives in `object-ui/`
 * - Existing callers and older tests can keep this stable path
 */

export * from "./object-ui";
