"use client";

/**
 * Compatibility shim.
 *
 * New placement/collision code belongs in `office/systems/occupancy-system`.
 * Keep this file only while older imports migrate.
 */

export * from "@/modules/office/systems/occupancy-system";
