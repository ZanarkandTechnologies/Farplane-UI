# TROUBLES

Append-only log for repeated failures, user corrections, and preventable misses.

Format:
`YYYY-MM-DD HH:mm Z | area,tags | request | miss | correction | prevention`

Promote only durable lessons from here into `docs/MEMORY.md` or the relevant skill/contract.

2026-06-15 17:06 Z | telemetry,hooks,codex | Telemetry tab had no recent events and Codex locks looked suspicious | Global Farplane Console ping hooks ran without `FARPLANE_CONVEX_SITE_URL` in the Codex app-server environment, and clean-env execution resolved a Python with broken HTTPS certificates; live Convex ingestion stopped while local Codex/session logs still captured recoverable rows | Updated `~/.codex/hooks.json` to source `~/.codex/config.local.env` before the ping hook and pin `/opt/homebrew/bin/python3`; backfilled `2026-06-14` onward with `npm run telemetry:import:aikage -- --since 2026-06-14 --site-url https://friendly-magpie-825.convex.site` | Hook smoke must be tested from a clean env and verified in Convex; importer runs should pass an explicit `--site-url` when shell env is not guaranteed.
2026-06-24 08:59 Z | telemetry,office,codex | Office employees disappeared while local hook events existed and Harness Usage showed no recent events | Rendering only consumed app-server threads or Convex `hookTelemetryEvents`; local `~/.farplane/events` rows were not part of the observed-worker path, and Vite did not derive the Convex client URL from the configured Convex site URL | Added a local observed Codex worker bridge that maps recent `~/.farplane/events` JSONL rows into read-only office workers, and made Vite derive `VITE_CONVEX_URL` from `.convex.site` URLs | When telemetry is the fallback source, prove the chain from local event file to UI provider state, not only from Convex dashboards.
2026-06-28 12:00 Z | settings,persistence,config | Office layout settings did not persist after Apply View | Started adding another browser localStorage key for office settings despite project-local config/sidecar rules and the existing config-file direction | Added an AGENTS invariant against new localStorage/env fallback surfaces and rerouted debugging toward the canonical config/sidecar state bridge | For settings persistence bugs, inspect the owning config/sidecar bridge first; do not create new browser storage keys or env knobs.
2026-06-30 08:04 Z | telemetry,hooks,event-miner | User questioned why a hookless miner thread would have hooks to publish events | Miner child prompt disabled Codex hooks but still carried direct telemetry-publish instructions and endpoint wording, mixing hookless execution with publish responsibility | Reframed the contract so the child miner extracts and returns an artifact only while the Stop hook/server side publishes, and removed endpoint literals from the child prompt | For hookless child agents, disabling hooks is not enough; prompts must also forbid direct publish/API/secret access and keep endpoint details in the parent publisher.
2026-06-30 08:51 Z | mining,ui,cross-project | Timeline Open Mine Run for a Farplane project showed a stale Farplane-UI run instead of TASK-0250 | The UI carried only run/output ids into Thread Data, so the mining bridge resolved the default repo mine root and could render another project's artifact | Thread Data targets now carry projectPath from timeline row through team panel into the mining bridge, and stale targets clear when the active project changes | Cross-project artifact links must include the owning project path all the way to the local bridge; run ids and output ids are not globally scoped.
2026-07-14 02:20 +08 | office,activity-destinations,visual-correction | Activity destination rooms still looked irregular and cropped after enlarging their renderer-only patches | The visible shell grew to 6 x 5 while solver occupancy remained 4.6 x 3.6, and the whole landmark group still registered as a runtime obstacle | Unified solver and renderer on one 7 x 7 tile-aligned room contract, made the floor inherit the landmark palette, and removed activity rooms from runtime navigation obstacles | Room-scale visuals must share their dimensions with placement containment, while walkable presentation spaces must stay separate from runtime obstacle registration.
2026-07-14 03:38 +08 | office,activity-destinations,composition | Individually placing each enlarged room at the current office edge produced clipped appendages, then neutral corner modules left visible beige gaps between otherwise adjacent rooms | Placement still optimized landmarks one at a time and treated corner continuity as empty floor instead of composing the office as one building | Replaced destination overflow with a deterministic module-ring planner, preserved the full outer rectangle, included corner rooms in the contiguous strip, and grouped unused modules into one entry/apron | When repeated objects define the building silhouette, plan the whole composition first; per-object edge scoring cannot reliably produce architectural continuity.
2026-07-14 04:40 +08 | office,activity-destinations,compactness | The uniform room ring made the office much larger than its actual contents and left a visibly empty central area | The first ring planner treated the oversized preliminary floor mask as a minimum core instead of deriving the building from required tables, routes, decor, and room capacity | Replaced source-bound sizing with a content-driven two-pass core pack and whole-module retry while preserving persisted required-object anchors | Preliminary layout masks are search inputs, not architectural requirements; final building bounds should come from the content contract they must preserve.
2026-07-14 04:45 +08 | office,activity-destinations,visual-model | The compacted continuous ring still looked oversized and architectural because landmarks, walls, corners, and the outer floor all expanded together | The implementation used a literal room shell to communicate destination area when a floor zone was sufficient, then required those shells to form a complete ring | Replaced enlarged shells with authored-scale props on 6 x 6 colored zones and distributed them across three open rails without filling unused outer space | In a fixed isometric tool, communicate lightweight semantic rooms with floor ownership first; do not add enclosing architecture unless it serves interaction or navigation.
2026-07-14 14:35 +08 | office,visual-design,operator-feedback | Several room-size, prop-scale, palette, and compactness passes passed technical and visual QA, but the operator still rated the office F for empty space and mismatched interiors | The generic visual-design workflow allowed local polish to proceed without a complete scene audit, reviewable whole-office blockout, or operator acceptance gate; walkability and occupancy were treated too much like aesthetic proof | Added a sanitized owner-isolated eval suite and a conditional spatial-scene branch to the canonical visual-design skill; baseline improved from 1/4 A to 4/4 A while preserving the dashboard boundary | For spatial aesthetic work, engineering metrics are guardrails only; audit the actual scene, compare composition models, and get the operator to accept a blockout before another broad implementation pass.
2026-07-15 12:24 +08 | office,collision,activity-rooms,validation | Command-office furniture visibly overlapped and most activity rooms disappeared after repeated layout corrections | Quality checks measured placement proxies instead of rendered visual bounds, while only two of thirteen supported first-party activity destinations were durable | Removed oversized render-only commons decor, restored and persisted the complete canonical room catalog, and made browser quality proof fail on visual AABB intersections or missing/duplicate room kinds | Spatial validation must measure the geometry users see and treat first-party semantic inventory completeness as a hard gate.
2026-07-15 12:41 +08 | office,visual-integrity,reward-hacking | The collision correction removed architectural beams and replaced distinctive activity scenes with generic compact props | The implementation optimized collision and software-renderer metrics by deleting visual value instead of fitting valued geometry inside an honest spatial contract | Restored a bounds-owned command canopy and the full themed landmark scenes inside framed bays, then retained visual AABB, inventory, and end-to-end Story timing gates | Never satisfy spatial QA by deleting accepted visual richness; preserve the design intent and make rendered geometry share the validator's dimensions.
2026-07-15 12:56 +08 | office,landmarks,labels,canonical-state | Project names disappeared, legacy library/map landmarks duplicated their kit-owned rooms, and activity bays still read as small display niches | Command neighborhoods explicitly suppressed labels; manual mapping rendered legacy canonical IDs beside their stable kit replacements; the bay contract remained 3 x 2 | Added readable screen-space project nameplates, canonical-identity deduplication that preserves custom same-kind rooms, 5 x 4 room zones, and visual-footprint-aware kit placement | Persistent semantic replacements need identity migration at render and persistence boundaries, and spatial labels must be judged at the default camera rather than merely mounted in the DOM.
2026-07-15 14:22 +08 | office,activity-rooms,panel-bindings,furniture | Canonical activity rooms existed visually but most did nothing when selected, while sparse surviving furniture made the rooms feel unfinished | Room definitions carried skill bindings without internal-panel bindings and had no persisted room-furniture style contract | Mapped all thirteen room identities to the live internal-panel registry, hydrated missing metadata onto persisted rooms, and added an executive walnut lounge-chair and side-table grammar while preserving standalone furniture | A semantic room is incomplete unless its identity, action binding, furnishing style, persistence behavior, and browser inventory proof ship together.
