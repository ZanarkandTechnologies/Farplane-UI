version: 1

# Project-local presentation and realtime-session preferences. Keys must match
# the canonical agentId in the live company model; runtime identity stays there.
agents:
  main:
    name: Farplane
    title: Founder copilot
    background: Coordinates the company from the founder's office.
    portrait: ui/public/zanarkand-logo.png
    appearance:
      accent: "#e8752b"
      skinTone: "#c9825d"
      hairColor: "#2a1d19"
      eyebrows: angled
    voice:
      provider: deepgram
      model: aura-2
      voiceId: theia
    vision:
      mode: turn_snapshot

  farplane-improvement:
    name: Refine
    title: Improvement Lead
    background: Owns self-improvement runs, behavior evidence, and verified operating gains.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#0ea5e9", skinTone: "#d9a27e", hairColor: "#49372f", eyebrows: angled }
    voice: { provider: deepgram, model: aura-2, voiceId: apollo }
    vision: { mode: turn_snapshot }

  farplane-research:
    name: Sage
    title: Research Lead
    background: Grounds project decisions in current evidence and maintained sources.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#b58a52", skinTone: "#c98b68", hairColor: "#3d2c25", eyebrows: straight }
    voice: { provider: deepgram, model: aura-2, voiceId: athena }
    vision: { mode: turn_snapshot }

  farplane-production:
    name: Frame
    title: Creative Producer
    background: Turns approved ideas into clear production-ready content and media outputs.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#a78bfa", skinTone: "#b97856", hairColor: "#25242d", eyebrows: arched }
    voice: { provider: deepgram, model: aura-2, voiceId: theia }
    vision: { mode: turn_snapshot }

  farplane-qa:
    name: Proof
    title: QA Lead
    background: Tests critical paths and keeps product and completion claims honest.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#22d3ee", skinTone: "#d7a17a", hairColor: "#222d32", eyebrows: straight }
    voice: { provider: deepgram, model: aura-2, voiceId: odysseus }
    vision: { mode: turn_snapshot }

  farplane-harness:
    name: Rig
    title: Harness Advisor
    background: Shapes reliable agent harnesses and measurable operating loops.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#f59e0b", skinTone: "#e2ad86", hairColor: "#50372a", eyebrows: angled }
    voice: { provider: deepgram, model: aura-2, voiceId: apollo }
    vision: { mode: turn_snapshot }

  farplane-skills:
    name: Glyph
    title: Skills Architect
    background: Designs reusable skills and keeps their contracts maintainable.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#34d399", skinTone: "#a96e4d", hairColor: "#2b2925", eyebrows: arched }
    voice: { provider: deepgram, model: aura-2, voiceId: athena }
    vision: { mode: turn_snapshot }

  farplane-finance:
    name: Ledger
    title: Finance Director
    background: Protects runway, reviews spending, and turns financial signals into operator decisions.
    portrait: ui/public/zanarkand-logo.png
    appearance:
      accent: "#37c987"
      skinTone: "#d7a17a"
      hairColor: "#2c2725"
      eyebrows: straight
    voice:
      provider: deepgram
      model: aura-2
      voiceId: odysseus
    vision:
      mode: turn_snapshot

  farplane-people:
    name: Scout
    title: People Operations
    background: Owns hiring pipelines, human contractor coordination, and team capacity planning.
    portrait: ui/public/zanarkand-logo.png
    appearance:
      accent: "#55a7ff"
      skinTone: "#b97856"
      hairColor: "#1e2028"
      eyebrows: arched
    voice:
      provider: deepgram
      model: aura-2
      voiceId: athena
    vision:
      mode: turn_snapshot

  farplane-office-manager:
    name: Steward
    title: Office Manager
    background: Maintains the office environment, equipment, layout, and operational readiness.
    portrait: ui/public/zanarkand-logo.png
    appearance:
      accent: "#f2a65a"
      skinTone: "#e5b18a"
      hairColor: "#5b3528"
      eyebrows: angled
    voice:
      provider: deepgram
      model: aura-2
      voiceId: apollo
    vision:
      mode: turn_snapshot

  farplane-comms:
    name: Relay
    title: Communications Lead
    background: Keeps operator communications clear, timely, and correctly routed.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#38bdf8", skinTone: "#d19a76", hairColor: "#382b28", eyebrows: arched }
    voice: { provider: deepgram, model: aura-2, voiceId: theia }
    vision: { mode: turn_snapshot }

  farplane-usage:
    name: Gauge
    title: Usage Analyst
    background: Watches agent-hour usage and operational telemetry signals.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#4ade80", skinTone: "#bc805d", hairColor: "#262b2d", eyebrows: straight }
    voice: { provider: deepgram, model: aura-2, voiceId: odysseus }
    vision: { mode: turn_snapshot }

  farplane-mining:
    name: Vein
    title: Mining Analyst
    background: Extracts structured evidence from task and thread history.
    portrait: ui/public/zanarkand-logo.png
    appearance: { accent: "#c084fc", skinTone: "#e0aa83", hairColor: "#342a39", eyebrows: angled }
    voice: { provider: deepgram, model: aura-2, voiceId: apollo }
    vision: { mode: turn_snapshot }
