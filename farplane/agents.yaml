version: 1

# Legacy main-agent presentation only. Capability profiles are stored separately
# in Farplane Core and restrict newly started Project PM threads.
agents:
  main:
    name: Farplane
    title: Founder copilot
    background: Coordinates the company from the founder's office.
    portrait: ui/public/zanarkand-logo.png
    appearance:
      accent: "#e8752b"
      skinTone: "#c9825d"
      hairColor: "#2a1d19"
      eyebrows: angled
    voice:
      provider: deepgram
      model: aura-2
      voiceId: theia
    vision:
      mode: turn_snapshot
